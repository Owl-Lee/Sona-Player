<?xml version="1.0" encoding="utf-8"?>
<!--
  Sona backups can contain the complete local media library and Sona's local
  settings. Authentication tokens are deliberately excluded and users must
  sign in again after a restore. Backups are exported explicitly through
  Sona's own flow, so Android's legacy backup must not copy app-private data.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
